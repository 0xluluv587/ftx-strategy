<!DOCTYPE html>
<html lang="zh-CN">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
<title>专项回款管理计划 - 产品原型 v4</title>
<style>
  * { margin: 0; padding: 0; box-sizing: border-box; }
  body { font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 'Helvetica Neue', Arial, sans-serif; background: #f0f0f0; display: flex; justify-content: center; align-items: flex-start; min-height: 100vh; padding: 20px 0; }
  .phone-frame { width: 390px; height: 844px; background: #fff; border-radius: 44px; box-shadow: 0 20px 60px rgba(0,0,0,0.15), inset 0 0 0 2px #d1d1d6; overflow: hidden; position: relative; }
  .status-bar { height: 54px; display: flex; align-items: flex-end; justify-content: space-between; padding: 0 30px 6px; font-size: 15px; font-weight: 600; color: #000; background: #fff; position: relative; z-index: 100; }
  .status-bar .time { font-weight: 700; }
  .status-bar .icons { display: flex; gap: 5px; align-items: center; font-size: 13px; }
  .screens-wrapper { height: 790px; overflow: hidden; position: relative; }
  .screen { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: #fff; overflow-y: auto; overflow-x: hidden; transform: translateX(100%); transition: transform 0.35s cubic-bezier(0.4, 0, 0.2, 1); -webkit-overflow-scrolling: touch; }
  .screen.active { transform: translateX(0); z-index: 10; }
  .screen.prev { transform: translateX(-30%); z-index: 5; }
  .screen::-webkit-scrollbar { display: none; }
  .nav-bar { height: 52px; display: flex; align-items: center; padding: 0 16px; background: #fff; position: sticky; top: 0; z-index: 50; border-bottom: 0.5px solid rgba(0,0,0,0.06); }
  .nav-bar .back-btn { width: 32px; height: 32px; display: flex; align-items: center; justify-content: center; cursor: pointer; font-size: 22px; color: #000; background: none; border: none; }
  .nav-bar .title { flex: 1; text-align: center; font-size: 17px; font-weight: 600; color: #000; margin-right: 32px; }
  .nav-bar .right-btn { font-size: 15px; color: #000; cursor: pointer; background: none; border: none; position: absolute; right: 16px; }
  .section-card { background: #fff; margin: 12px 16px; border-radius: 16px; padding: 20px; box-shadow: 0 1px 4px rgba(0,0,0,0.04); }
  .page-bg { background: #f5f6f8; }
  .page-bg-warm { background: linear-gradient(180deg, #fefcf7 0%, #f5f6f8 40%); }
  .label-sm { font-size: 13px; color: #8c8c8c; line-height: 1.5; }
  .value-lg { font-size: 28px; font-weight: 700; color: #1a1a1a; letter-spacing: -0.5px; }
  .text-green { color: #00b578; }
  .text-red { color: #e54d42; }
  .text-orange { color: #f0883a; }
  .text-blue { color: #1677ff; }
  .text-gold { color: #c49b5e; }
  .divider { height: 0.5px; background: #f0f0f0; margin: 16px 0; }
  .btn-primary { width: 100%; height: 52px; border-radius: 26px; border: none; background: linear-gradient(135deg, #1a1a1a, #333); color: #fff; font-size: 17px; font-weight: 600; cursor: pointer; transition: all 0.2s; letter-spacing: 0.5px; }
  .btn-primary:active { transform: scale(0.97); opacity: 0.9; }
  .btn-primary.gold { background: linear-gradient(135deg, #c49b5e, #d4ad70); }
  .btn-primary.disabled { background: #e0e0e0; color: #aaa; cursor: not-allowed; pointer-events: none; }
  .btn-secondary { width: 100%; height: 52px; border-radius: 26px; border: 1.5px solid #e0e0e0; background: #fff; color: #1a1a1a; font-size: 17px; font-weight: 600; cursor: pointer; transition: all 0.2s; }
  .btn-secondary:active { background: #f5f5f5; }
  .bottom-action { position: sticky; bottom: 0; padding: 12px 16px 28px; background: linear-gradient(0deg, #fff 80%, transparent); z-index: 20; }
  .timeline { padding: 0 4px; }
  .timeline-item { display: flex; gap: 16px; position: relative; padding-bottom: 24px; }
  .timeline-item:last-child { padding-bottom: 0; }
  .timeline-dot-col { display: flex; flex-direction: column; align-items: center; width: 20px; flex-shrink: 0; }
  .timeline-dot { width: 12px; height: 12px; border-radius: 50%; border: 2.5px solid #00b578; background: #fff; flex-shrink: 0; margin-top: 4px; }
  .timeline-dot.active { background: #00b578; }
  .timeline-dot.gold { border-color: #c49b5e; }
  .timeline-dot.gold.active { background: #c49b5e; }
  .timeline-line { width: 1.5px; flex: 1; background: #e8e8e8; margin-top: 6px; }
  .timeline-item:last-child .timeline-line { display: none; }
  .timeline-content { flex: 1; }
  .timeline-title { font-size: 15px; font-weight: 600; color: #1a1a1a; margin-bottom: 4px; }
  .timeline-desc { font-size: 13px; color: #8c8c8c; line-height: 1.5; }
  .timeline-amount { font-size: 15px; font-weight: 600; color: #00b578; margin-top: 4px; }
  .timeline-note-link { font-size: 13px; color: #1677ff; margin-top: 4px; display: inline-flex; align-items: center; gap: 4px; cursor: pointer; }
  .timeline-note-link.muted { color: #b0b0b0; }
  .info-row { display: flex; justify-content: space-between; align-items: center; padding: 12px 0; }
  .info-row .label { font-size: 14px; color: #8c8c8c; }
  .info-row .value { font-size: 14px; font-weight: 500; color: #1a1a1a; text-align: right; }
  .info-row-border { border-bottom: 0.5px solid #f0f0f0; }
  .tabs { display: flex; padding: 0 16px; border-bottom: 0.5px solid #f0f0f0; background: #fff; position: sticky; top: 52px; z-index: 40; }
  .tab { flex: 1; text-align: center; padding: 14px 0; font-size: 15px; font-weight: 500; color: #8c8c8c; cursor: pointer; position: relative; transition: color 0.2s; background: none; border: none; }
  .tab.active { color: #1a1a1a; font-weight: 600; }
  .tab.active::after { content: ''; position: absolute; bottom: 0; left: 50%; transform: translateX(-50%); width: 24px; height: 3px; background: #1a1a1a; border-radius: 1.5px; }
  .holding-card { margin: 12px 16px; background: #fff; border-radius: 16px; padding: 20px; box-shadow: 0 1px 4px rgba(0,0,0,0.04); cursor: pointer; transition: all 0.2s; }
  .holding-card:active { transform: scale(0.98); }
  .holding-header { display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px; }
  .holding-name { font-size: 17px; font-weight: 700; color: #1a1a1a; }
  .holding-badge { padding: 3px 10px; border-radius: 10px; font-size: 12px; font-weight: 500; background: #e8f8ef; color: #00b578; }
  .nav-chart-placeholder { height: 160px; background: linear-gradient(180deg, #f0fdf4 0%, #fff 100%); border-radius: 12px; display: flex; align-items: center; justify-content: center; margin: 12px 0; position: relative; overflow: hidden; }
  .nav-chart-placeholder svg { width: 100%; height: 100%; }
  .modal-overlay { position: fixed; top: 0; left: 0; right: 0; bottom: 0; background: rgba(0,0,0,0.5); z-index: 1000; display: none; justify-content: center; align-items: flex-end; }
  .modal-overlay.show { display: flex; }
  .modal-sheet { background: #fff; border-radius: 24px 24px 0 0; width: 390px; padding: 24px 20px 36px; max-height: 80vh; overflow-y: auto; animation: slideUp 0.3s ease; }
  @keyframes slideUp { from { transform: translateY(100%); } to { transform: translateY(0); } }
  .modal-handle { width: 40px; height: 4px; background: #e0e0e0; border-radius: 2px; margin: 0 auto 20px; }
  .toast { position: fixed; top: 80px; left: 50%; transform: translateX(-50%); background: rgba(0,0,0,0.8); color: #fff; padding: 12px 24px; border-radius: 24px; font-size: 14px; z-index: 2000; opacity: 0; transition: opacity 0.3s; pointer-events: none; }
  .toast.show { opacity: 1; }
  .success-icon { width: 72px; height: 72px; border-radius: 50%; background: linear-gradient(135deg, #00b578, #34d399); display: flex; align-items: center; justify-content: center; margin: 40px auto 24px; }
  .success-icon svg { width: 36px; height: 36px; }
  .risk-tag { display: inline-flex; align-items: center; gap: 4px; padding: 4px 10px; border-radius: 8px; font-size: 12px; font-weight: 500; }
  .risk-low { background: #e8f8ef; color: #00b578; }
  .vip-tag { background: linear-gradient(135deg, #c49b5e, #d4ad70); color: #fff; padding: 3px 10px; border-radius: 8px; font-size: 11px; font-weight: 600; }
  .icon-actions { display: flex; gap: 0; margin: 20px 0 8px; }
  .icon-action { flex: 1; text-align: center; cursor: pointer; background: none; border: none; padding: 8px 0; }
  .icon-action-circle { width: 44px; height: 44px; border-radius: 50%; background: #f5f6f8; display: flex; align-items: center; justify-content: center; margin: 0 auto 8px; transition: background 0.2s; }
  .icon-action:active .icon-action-circle { background: #e8e8e8; }
  .icon-action-label { font-size: 12px; color: #666; }
  .disclaimer { font-size: 12px; color: #b0b0b0; line-height: 1.7; padding: 16px 20px; background: #fafafa; margin: 12px 16px; border-radius: 12px; }
  .comp-header { background: linear-gradient(180deg, #fefaf3 0%, #fff 100%); padding: 24px 20px; text-align: center; }
  .comp-round-badge { display: inline-block; padding: 4px 14px; border-radius: 12px; b